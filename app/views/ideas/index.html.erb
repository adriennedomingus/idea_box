<table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th>Quality</th>
  </tr>
  <% @ideas.each do |idea| %>
    <tr>
      <td><%= idea.title %></td>
      <td><%= idea.body %></td>
      <td><%= idea.quality %></td>
    </tr>
  <% end %>
</table>
